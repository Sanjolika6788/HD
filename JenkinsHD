pipeline {
    agent any

    stages {
        stage('Checkout') {
            steps {
                git branch: 'main', url: 'https://github.com/Sanjolika6788/HD.git'
            }
        }
        stage('Build') {
            steps {
                script {
                    // Example for a Maven project on Windows
                    bat 'mvn clean install'
                }
            }
        }
        stage('Test') {
            steps {
                script {
                    // Example for running JUnit tests on Windows
                    bat 'mvn test'
                }
            }
        }
        stage('Code Quality') {
            steps {
                script {
                    // Example for running SonarQube analysis on Windows
                    bat 'mvn sonar:sonar'
                }
            }
        }
        stage('Deploy to Staging') {
            steps {
                script {
                    // Example for Docker on Windows
                    bat 'docker build -t my-app .'
                    bat 'docker run -d -p 8080:8080 my-app'
                }
            }
        }
        stage('Release to Production') {
            steps {
                script {
                    // Example for AWS CodeDeploy on Windows
                    bat 'aws deploy create-deployment --application-name my-app --deployment-group my-deployment-group --s3-location bucket=my-bucket,key=my-app.zip,bundleType=zip'
                }
            }
        }
        stage('Monitor') {
            steps {
                script {
                    // Example for Datadog on Windows
                    bat 'datadog-agent status'
                }
            }
        }
    }
}
